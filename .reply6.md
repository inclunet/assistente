Nova rodada do Bugbot: um achado, procedente.

**O estado da sessão compartilhava memória com quem escuta.** O `setConfigOptions` guardava a referência que chega, e no caminho de streaming esse slice é o mesmo que segue no `Update` para o consumidor. Quem retivesse o `Update` conseguiria trocar o modelo da conversa sem passar pelo agente. O caminho inverso também doía: a troca de modo mexe nas opções no lugar, então alteraria por baixo dos panos um `Update` já entregue.

Corrigido guardando cópia funda no `setConfigOptions`, que é o ponto por onde todo mundo passa (`deliver`, `SetConfigOption` e a criação da sessão). É a mesma decisão que o `ConfigOptions()` já tomava na leitura — agora ela vale nos dois sentidos, com o `copyOptions` compartilhado.

Teste novo `TestOEstadoDaSessaoNaoCompartilhaMemoriaComQuemEscuta`. Vale registrar que a primeira versão dele passava mesmo com o defeito: o slice externo já se separava por acaso, porque preservar o modo faz um `append` que realoca. O que continuava compartilhado era a lista de valores dentro de cada opção — a lista de modelos. Com a asserção sobre ela, o teste falha sem a correção.
